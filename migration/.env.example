# microCMS サービスID（xxxx.microcms.io の xxxx 部分）
MICROCMS_SERVICE_ID=your-service-id

# microCMS 書き込み用APIキー（GET/POST/PUT権限）。移行後は無効化/削除すること。
# ※このファイル(.env)はコミット禁止。.gitignore済み。
MICROCMS_WRITE_API_KEY=your-write-api-key

# 移行元WordPressのベースURL（通常は変更不要）
WP_BASE_URL=https://shu-web.jp
